#include <stdio.h>
/**
 * main - Starting point of the program
 * Return: always 0 to indicate success
 */
int main(void)
{
char c;
c = 'a';
while
(c <= 'z');
{
putchar(c);
c++;
}
putchar('\n');
return (0);
}
