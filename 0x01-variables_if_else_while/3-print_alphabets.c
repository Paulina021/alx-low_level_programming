#include <stdio.h>
/**
 * main - Starting point of the program
 * Return: always 0 to indicate success
 */
int main(void)
{
char lowercase;
lowercase = 'a';
while
(lowercase <= 'z');
{
putchar(lowercase);
lowercase++;
}
putchar('\n');
return (0);
}
