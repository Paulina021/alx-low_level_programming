#include <stdio.h>
/**
 * main - Starting point of the program
 * Return: Always 0 to indicate success
 */
int main(void)
{
char b;
b = 'a';
while (b <= 'z')
{
	putchar(b);
	c++;
}
	putchar('\n');
	return (0);
}
